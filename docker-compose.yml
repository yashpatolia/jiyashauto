services:
  app:
    build: .
    ports:
      - "3001:3000"
    env_file:
      - .env
    volumes:
      - db_data:/app/db
      - ./static/uploads:/app/static/uploads
    restart: unless-stopped

volumes:
  db_data:
